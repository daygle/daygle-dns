# Rust
/target
**/*.rs.bk

# Frontend dependencies only (the built bundle in web/dist is committed so the
# server embeds a working dashboard without requiring Node).
web/node_modules

# Runtime artifacts
*.db
*.db-journal
*.db-wal
*.db-shm

# Local configuration / certificates
daygle.toml
certs/

# OS / editor
.DS_Store
*.swp

# Local app state
.freebuff/
